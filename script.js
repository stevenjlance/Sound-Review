// Step 1: Declare a variable that will hold the sound


function preload(){
	// Step 2: Load the sound using loadSound


}

function setup() {
  createCanvas(500, 500);
  frameRate(10)
}

function draw() {
  background("aqua");
	// Step 3: Play the sound when a condition is met (like the spacebar being pressed!)

}